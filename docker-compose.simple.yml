version: '3.8'

services:
  app:
    build: .
    ports:
      - "80:80"
      - "3000:3000"
    environment:
      - DB_TYPE=sqlite
      - DB_PATH=/app/data/industrial_dev.db
      - NODE_ENV=production
    volumes:
      - app_data:/app/data
    restart: unless-stopped

volumes:
  app_data:
