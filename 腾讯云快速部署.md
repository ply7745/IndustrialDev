# 🚀 腾讯云服务器 - 3分钟快速部署

## 只需3步，完成部署！

### 第1步：登录腾讯云服务器

```bash
ssh root@您的服务器IP
```

### 第2步：上传项目文件

在您的本地电脑上执行：

```bash
scp -r /path/to/workspace root@您的服务器IP:/root/
```

或者，如果您使用 Git，在服务器上执行：

```bash
git clone https://您的仓库地址.git
cd 仓库名
```

### 第3步：运行一键部署脚本

在服务器上执行：

```bash
cd /root/workspace  # 或您的项目目录
chmod +x deploy-tencent.sh
./deploy-tencent.sh
```

按照脚本提示选择数据库类型，等待部署完成！

---

## 📱 部署完成后

访问您的应用：**http://您的服务器IP**

---

## 🔧 常用命令

```bash
# 查看服务状态
docker-compose ps

# 查看日志
docker-compose logs -f

# 停止服务
docker-compose down

# 重启服务
docker-compose restart

# 更新应用
git pull
docker-compose up -d --build
```

---

## ⚠️ 重要提示

### 1. 配置腾讯云安全组

登录腾讯云控制台，配置安全组开放以下端口：
- **80** - HTTP访问
- **22** - SSH管理（必需）

### 2. 备份数据

数据存储在 Docker volumes 中，建议定期备份：

```bash
# 创建备份脚本
cat > /root/backup.sh << 'EOF'
#!/bin/bash
DATE=$(date +%Y%m%d_%H%M%S)
BACKUP_DIR="/root/backups"
mkdir -p $BACKUP_DIR
docker run --rm -v labor_data:/data -v $BACKUP_DIR:/backup alpine tar czf /backup/labor_data_$DATE.tar.gz -C /data .
find $BACKUP_DIR -name "labor_data_*.tar.gz" -mtime +7 -delete
EOF

chmod +x /root/backup.sh

# 设置定时备份（每天凌晨2点）
(crontab -l 2>/dev/null; echo "0 2 * * * /root/backup.sh") | crontab -
```

---

## 📚 更多信息

- [详细部署指南](file:///workspace/docs/腾讯云部署指南.md)
- [系统使用说明](file:///workspace/docs/员工信息管理系统使用说明.md)
- [项目设计文档](file:///workspace/docs/labor-management-system-design.md)

---

## ❓ 遇到问题？

1. 查看服务日志：`docker-compose logs -f`
2. 检查安全组配置
3. 确认防火墙状态
4. 查看详细部署文档
