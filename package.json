{
  "name": "industrial-dev",
  "version": "1.0.0",
  "private": true,
  "description": "工业信息化系统开发框架 - MES/WMS/ERP",
  "scripts": {
    "dev": "pnpm --filter './apps/*' dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "clean": "turbo run clean",
    "db:init": "tsx scripts/init-db.ts",
    "db:seed": "tsx scripts/seed-db.ts"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "typescript": "^5.3.0"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=20.0.0"
  }
}